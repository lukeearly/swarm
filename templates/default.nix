{
    empty = {
        path = ./empty;
        description = "Empty swarm specification";
    };

    load-balancer = {
        path = ./load-balancer;
        description = "Specification for a swarm with primary and backup web servers and an nginx load balancer";
    };
}